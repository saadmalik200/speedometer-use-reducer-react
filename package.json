{
  "name": "spa-store-use_reducer",
  "version": "0.1.0",
  "homepage": "https://saadmalik200.github.io/speedometer-use-reducer-react",
  "private": true,
  "dependencies": {
    "react": "17.0.0",
    "react-d3-speedometer": "^1.1.0",
    "react-dom": "17.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "deploy2": "gh-pages -d build -o codecommit",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.18",
    "gh-pages": "^4.0.0",
    "tailwindcss": "^3.2.1"
  }
}
